DATABASE_URL=postgres://postgres:postgres@localhost:5432/plakk

WORKOS_API_KEY=sk_test_...
WORKOS_CLIENT_ID=client_...

# Polar is the paid-access source of truth. Use sandbox credentials locally.
POLAR_ACCESS_TOKEN=polar_oat_...
POLAR_ACCESS_BENEFIT_ID=
POLAR_ENVIRONMENT=sandbox
# Comma-separated products shown as checkout choices, e.g. monthly and yearly.
POLAR_PRODUCT_IDS=
REDIS_URL=redis://localhost:6379

# Do not add host, port, origin, or redirect values here. `vp run dev` derives them.
