{
  "name": "@plakk/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --experimental-strip-types --watch src/main.ts",
    "build": "vp pack src/main.ts --format esm --out-dir dist",
    "start": "node dist/main.mjs",
    "test": "vp test run",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-beta.92",
    "@plakk/db": "workspace:*",
    "@plakk/shared": "workspace:*",
    "@workos-inc/node": "^10.7.0",
    "dotenv": "^17.4.2",
    "effect": "catalog:",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@effect/vitest": "4.0.0-beta.92",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
