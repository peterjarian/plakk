{
  "name": "@plakk/backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp node --env-file-if-exists=.env src/main.ts",
    "build": "vp pack src/main.ts --format esm --out-dir dist",
    "start": "node dist/main.mjs",
    "test": "vp test run",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@effect/platform-node": "4.0.0-beta.92",
    "@plakk/db": "workspace:*",
    "@plakk/shared": "workspace:*",
    "@workos-inc/node": "^10.7.0",
    "effect": "catalog:",
    "jose": "^6.2.3"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
